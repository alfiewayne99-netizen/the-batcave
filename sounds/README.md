# Sound Assets

Place notification sounds here:
- `start.mp3` - Played when an agent starts working
- `complete.mp3` - Played when an agent completes a task
- `error.mp3` - Played when an error occurs

Sounds are generated via Web Audio API if files are not present.
